matrix:
  include:


    - language: python
      os: linux
      python: 3.7
      before_script:
        - cd python-bindings
        - bash install_rust.sh
        - pip install -r requirements-dev.txt
      script:
        - python setup.py install